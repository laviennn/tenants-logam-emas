---
import Register from '../../../components/pages/Register.astro';

export async function getStaticPaths() {
  return [
    { params: { lang: 'en' } },
    { params: { lang: 'my' } },
  ];
}
---
<Register />
